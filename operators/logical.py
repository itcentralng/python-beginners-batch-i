# OR AND = Logical operators allow us
# to chain multiple other operations

name = "Khalifa"
age = 19

print(('a' in name) and (age > 108))
print(('a' in name) or (age > 108))
print(('a' not in name) or (age > 108))